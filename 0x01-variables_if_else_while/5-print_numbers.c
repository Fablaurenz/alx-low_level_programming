#include <stdio.h>
/**
 * main - Print numbers fom 0 to 9
 *
 * Return: A;ways 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 0; i < 9; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
